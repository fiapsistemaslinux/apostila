; Configuracao de apontamentos do dominio {{ domain }}:
;
$TTL 24h ; Tempo padrao para todos os registros sem TTL
@	IN	SOA	ns1.{{ domain }}.	helpdesk@{{ domain }}.	(
		{{ serial }} ; serial
		8h ; refresh
		1h ; retry
		3d ; expire
		3h ; negative cache ttl
		)

@		IN	NS	ns1
ns1		IN	A	{{ regmx }}

{{ rvaddr }}	IN	PTR	mail.{{ domain }}.
